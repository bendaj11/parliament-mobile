import type { AtlasConfig } from "@atlas/contracts" with { "resolution-mode": "import" };

export default {
  id: "mobile-host",
  name: "Mobile Host",
  framework: "angular"
} satisfies AtlasConfig;
